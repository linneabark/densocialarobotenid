<ScheduleScreenSix>
    Image:
        allow_stretch: True
        source: 'Images/schedule_pics/vecka6.png'
        size: self.size
        #Button:
        #    background_color: 0,0,0,0
        #    on_touch_down:
         #       app.root.transition = SlideTransition(direction = 'left')
         #       root.manager.current = "appview"

        Button:
            background_normal: 'Images/appviewbutton.png'
            background_down: 'Images/appviewbutton.png'
            size_hint: None, None
            size: 150, 150
            pos: (1350,700)
            on_press:
                root.manager.transition.direction = "left"
                root.manager.transition.duration = 1
                root.manager.current = "appview"

        Button:
            background_normal: 'Images/leftarrowbutton.png'
            background_down: 'Images/leftarrowbutton.png'
            pos: (0,350)
            on_press:
                root.manager.transition.direction = "right"
                root.manager.transition.duration = 1
                root.manager.current = "s5"