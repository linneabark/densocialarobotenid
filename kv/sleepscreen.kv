<SleepScreen>
    Image:
        allow_stretch: True
        source: 'tenor.gif'
        size: self.size
        Button:
            background_color: 0,0,0,0
            on_touch_down:
                app.root.transition = FadeTransition()
                root.manager.current = "main"

