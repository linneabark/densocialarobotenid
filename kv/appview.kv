<Appview>
   #Change backgroundcolor
    #canvas:
       # Color:
          #  rgba:
       # Rectangle:
       #     size: self.size
        #    pos: self.pos
    Image:
        source: 'Images/title5.png'
        pos: root.x, root.height/3
    GridLayout:
        spacing: 100
        cols: 4
        padding: 180,250
        Button:
            #center: self.parent.center
            font_size: 28
            size_hint: None, None
            size: 300, 300
            #pos: (10,100)
            background_normal: 'Images/schemaknapp3.png'
            #background_color: (1, .3, .4, .85)
            on_press:
                app.root.transition = SlideTransition(direction = 'right')
                root.manager.current =  "schedule"

        Button:
            #center: self.parent.center
            font_size: 28
            size_hint: None, None
            size: 300, 300
            background_normal: 'Images/RPS3.png'
            #pos: (10,100)
            #background_color: (1, .3, .4, .85)
            on_press:
                #app.root.transition = SlideTransition(direction = 'up')
                root.manager.current =  "one"
                #root.launchRPS()

        Button:
            #center: self.parent.center
            font_size: 28
            size_hint: None, None
            size: 300, 300
            #pos: (10,100)
            background_normal: 'Images/mattknapp4.png'
            #background_color: (1, .3, .4, .85)
            on_press:
                app.root.transition = SlideTransition(direction = 'left')
                root.manager.current =  "math"