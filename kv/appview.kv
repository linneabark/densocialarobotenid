<Appview>
    #canvas.before:
     #   Rectangle:
      #      pos: self.pos
       #     size: self.size
    #Image:
     #   source: "Images/redbackground.png"
      #  keep_ratio: False
       # allow_stretch: True

    BoxLayout:
        orientation: 'vertical'
        padding: 40
        spacing: 20
        GridLayout:
            cols: 8
            rows: 1
            size_hint: 1, .2
            spacing: 10

            Button:
                #size_hint: .1, .1
                background_normal: 'voiceknapp4.png'
                background_down: 'voiceknapp4.png'
                on_press:
                    app.root.transition = SlideTransition(direction = 'down')
                    #root.manager.current =  "main"
                    root.listen()

            Label:
                text: ""

            Label:
                text: ""

            Label:
                text: ""

            Label:
                text: ""

            Label:
                text: ""

            Label:
                text: ""

            Button:
                background_normal: 'Images/shutdownbutton.png'
                background_down: 'Images/shutdownbutton.png'
                #background_color: (1, .3, .4, .85)
                on_press: app.quit()



        GridLayout:
            cols: 3
            spacing: 50
            #padding: 20

            Button:
                #center: self.parent.center
                #pos: (10,100)
                background_normal: 'Images/schedulebuttons/schemaknapp61.png'
                background_down: 'Images/schedulebuttons/schemaknapp61.png'
                #background_color: (1, .3, .4, .85)
                on_press:
                    app.root.transition = SlideTransition(direction = 'right')
                    root.manager.current =  "schedule"

            Button:
                #center: self.parent.center
                #font_size: 28
                background_normal: 'Images/RPSbuttons/RPS5.png'
                background_down: 'Images/RPSbuttons/RPS5.png'
                #pos: (10,100)
                #background_color: (1, .3, .4, .85)
                on_press:
                    app.root.transition = SlideTransition(direction = 'up')
                    root.manager.current =  "one"
                    #root.launchRPS()

            Button:
                #center: self.parent.center
                #font_size: 28
                #pos: (10,100)
                background_normal: 'Images/mathbuttons/mattknapp5.png'
                background_down: 'Images/mathbuttons/mattknapp5.png'
                #background_color: (1, .3, .4, .85)
                on_press:
                    app.root.transition = SlideTransition(direction = 'left')
                    root.manager.current =  "calculator"

