<Appview>
   #Change backgroundcolor
    #canvas:
       # Color:
          #  rgba:
       # Rectangle:
       #     size: self.size
        #    pos: self.pos
    Image:
        source: 'title5.png'
        pos: root.x, root.height/3
    GridLayout:
        spacing: 100
        cols: 4
        padding: 180,250
        Button:
            #center: self.parent.center
            font_size: 28
            size_hint: None, None
            size: 300, 300
            #pos: (10,100)
            background_normal: 'schemaknapp.png'
            #background_color: (1, .3, .4, .85)
            on_press:
                app.root.transition = SlideTransition(direction = 'up')
                root.manager.current =  "schedule"

        Button:
            #center: self.parent.center
            font_size: 28
            size_hint: None, None
            size: 300, 300
            #pos: (10,100)
            background_normal: 'matteknapp.png'
            #background_color: (1, .3, .4, .85)
            on_press:
                app.root.transition = SlideTransition(direction = 'up')
                root.manager.current =  "math"

        Button:
            #center: self.parent.center
            font_size: 28
            size_hint: None, None
            size: 300, 300
            #pos: (10,100)
            background_normal: 'RPSps.png'
            #background_color: (1, .3, .4, .85)
            on_press:
                app.root.transition = SlideTransition(direction = 'up')
                root.manager.current =  "rps"